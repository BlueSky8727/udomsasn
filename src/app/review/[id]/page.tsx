// src/app/review/[id]/page.tsx
import{notFound}from'next/navigation';import{AppShell}from'@/components/ui/app-shell';import{ReviewWorkspace}from'@/components/review/review-workspace';import{getViewerRole}from'@/lib/auth';import{backendFetch,toReviewJob}from'@/lib/backend';export default async function ReviewPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const role=await getViewerRole();try{const job=toReviewJob(await backendFetch<any>(`/media/${id}`));return <AppShell role={role}><ReviewWorkspace job={job} role={role}/></AppShell>}catch{notFound()}}
