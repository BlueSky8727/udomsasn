// src/app/browse/page.tsx
import {AppShell} from '@/components/ui/app-shell';import{MediaBrowser}from'@/components/library/media-browser';import{PageHeading}from'@/components/ui/page-heading';import{getViewerRole}from'@/lib/auth';import{backendFetch,toDemoMedia}from'@/lib/backend';
export default async function BrowsePage(){const role=await getViewerRole();let approved:any[]=[];try{approved=(await backendFetch<any[]>('/media/public')).map(toDemoMedia)}catch{}return <AppShell role={role}><PageHeading eyebrow="Media Library" title="ค้นหาสื่อการสอน" description="ค้นหาสื่อที่ผ่านการตรวจอนุมัติแล้ว พร้อมกรองตามวิชาและระดับชั้น"/><MediaBrowser media={approved}/></AppShell>}
